---
  name: Guard Main Branch

  on:
    pull_request:
      types:
        - auto_merge_enabled
      branches:
        - main
    merge_group:
      branches:
        - main

  permissions:
    contents: read

  jobs:
    guardian-jobs:
      uses: .github/workflows/guard_template.yml
      secrets: inherit